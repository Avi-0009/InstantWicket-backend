services:
  db:
    container_name: postgres
    image: "postgres:18"
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=1234
      - POSTGRES_DB=InstantWicket
networks:
  api.network: